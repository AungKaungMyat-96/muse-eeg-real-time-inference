# Git Full Inference Checkpoint

## Recommended Commands

```powershell
cd C:\Users\Lenovo\Documents\muse-eeg-real-time-inference
git status
git add .
git commit -m "Milestone 9 PASS: full end-to-end FPGA CNN inference verified"
git push origin main
```

## Important Note

Vivado working files are under:

- `D:\Users\Lenovo\project_1\muse_cnn_sources`

If these files are not inside the Git repository, copy/sync required source files into the repo before commit.
