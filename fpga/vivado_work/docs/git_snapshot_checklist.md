# Git Snapshot Checklist

## Commands

```bash
git status
git add .
git commit -m "Milestone 4A PASS: Conv1 ReLU MaxPool Conv2 verified"
git push origin main
```

## Verify Before Push

- `src/`
- `tb/`
- `scripts/`
- `constraints/`
- `docs/`
- `*.tcl`
